<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Knot & Stitches | Handmade Crochet Magic ✨</title>
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Fredoka:wght@400;600;700&family=Quicksand:wght@400;600;700&display=swap" rel="stylesheet">
  
  <style>
    /* --------------------------------------------------
       COLOR PALETTE & GLOBAL VARIABLES (From Image)
    -------------------------------------------------- */
    :root {
      --amaranth: #933B5B;
      --thulian-pink: #B5728A;
      --brook-green: #AABAAE;
      --chalk: #E3D6BF;
      --pomelo-olive: #9F9679;
      
      --bg-cream: #FAF7F2;
      --card-bg: #FFFFFF;
      --text-dark: #4A3E3D;
      --border-radius: 20px;
      --shadow: 0 8px 20px rgba(147, 59, 91, 0.08);
      --transition: all 0.3s cubic-bezier(0.25, 0.8, 0.25, 1);
    }

    * {
      box-sizing: border-box;
      margin: 0;
      padding: 0;
    }

    body {
      font-family: 'Quicksand', sans-serif;
      background-color: var(--bg-cream);
      color: var(--text-dark);
      line-height: 1.6;
    }

    h1, h2, h3, h4 {
      font-family: 'Fredoka', cursive;
      color: var(--amaranth);
    }

    a {
      text-decoration: none;
      color: inherit;
    }

    /* --------------------------------------------------
       HEADER & NAVIGATION
    -------------------------------------------------- */
    header {
      background-color: var(--card-bg);
      padding: 1.2rem 8%;
      display: flex;
      justify-content: space-between;
      align-items: center;
      position: sticky;
      top: 0;
      z-index: 100;
      box-shadow: 0 4px 15px rgba(0, 0, 0, 0.03);
      border-bottom: 3px solid var(--chalk);
    }

    .logo {
      font-size: 1.8rem;
      font-weight: 700;
      color: var(--amaranth);
      display: flex;
      align-items: center;
      gap: 0.5rem;
    }

    nav ul {
      display: flex;
      list-style: none;
      gap: 2rem;
    }

    nav a {
      font-weight: 600;
      font-size: 1.05rem;
      color: var(--pomelo-olive);
      transition: var(--transition);
    }

    nav a:hover {
      color: var(--amaranth);
    }

    /* --------------------------------------------------
       HERO SECTION
    -------------------------------------------------- */
    .hero {
      background: linear-gradient(135deg, rgba(227, 214, 191, 0.4) 0%, rgba(170, 186, 174, 0.3) 100%);
      padding: 5rem 8%;
      text-align: center;
      border-radius: 0 0 40px 40px;
      margin-bottom: 4rem;
    }

    .hero-badge {
      background-color: var(--thulian-pink);
      color: white;
      padding: 0.4rem 1.2rem;
      border-radius: 50px;
      font-size: 0.9rem;
      font-weight: 600;
      display: inline-block;
      margin-bottom: 1rem;
    }

    .hero h1 {
      font-size: 3rem;
      margin-bottom: 1rem;
      color: var(--amaranth);
    }

    .hero p {
      font-size: 1.2rem;
      max-width: 600px;
      margin: 0 auto 2rem;
      color: var(--text-dark);
    }

    .btn-primary {
      background-color: var(--amaranth);
      color: white;
      padding: 0.9rem 2rem;
      border: none;
      border-radius: 50px;
      font-size: 1rem;
      font-weight: 600;
      cursor: pointer;
      transition: var(--transition);
      box-shadow: 0 4px 12px rgba(147, 59, 91, 0.3);
      display: inline-block;
    }

    .btn-primary:hover {
      background-color: var(--thulian-pink);
      transform: translateY(-2px);
    }

    /* --------------------------------------------------
       CONTAINER & SECTIONS
    -------------------------------------------------- */
    .container {
      max-width: 1200px;
      margin: 0 auto;
      padding: 0 2rem;
    }

    .section-title {
      text-align: center;
      font-size: 2.2rem;
      margin-bottom: 0.5rem;
    }

    .section-subtitle {
      text-align: center;
      color: var(--pomelo-olive);
      margin-bottom: 2.5rem;
      font-weight: 600;
    }

    /* --------------------------------------------------
       PRODUCTS SECTION & FILTERS
    -------------------------------------------------- */
    .products-section {
      margin-bottom: 6rem;
    }

    .filter-buttons {
      display: flex;
      justify-content: center;
      gap: 0.8rem;
      margin-bottom: 2.5rem;
      flex-wrap: wrap;
    }

    .filter-btn {
      background-color: var(--card-bg);
      border: 2px solid var(--chalk);
      color: var(--pomelo-olive);
      padding: 0.6rem 1.4rem;
      border-radius: 50px;
      font-weight: 600;
      cursor: pointer;
      transition: var(--transition);
    }

    .filter-btn:hover,
    .filter-btn.active {
      background-color: var(--brook-green);
      border-color: var(--brook-green);
      color: white;
    }

    .product-grid {
      display: grid;
      grid-template-columns: repeat(auto-fill, minmax(260px, 1fr));
      gap: 2rem;
    }

    .product-card {
      background-color: var(--card-bg);
      border-radius: var(--border-radius);
      overflow: hidden;
      box-shadow: var(--shadow);
      transition: var(--transition);
      border: 2px solid rgba(227, 214, 191, 0.3);
      display: flex;
      flex-direction: column;
    }

    .product-card:hover {
      transform: translateY(-6px);
      box-shadow: 0 12px 25px rgba(147, 59, 91, 0.15);
    }

    .product-img {
      width: 100%;
      height: 220px;
      background-color: var(--chalk);
      display: flex;
      align-items: center;
      justify-content: center;
      font-size: 4rem;
      position: relative;
    }

    .product-tag {
      position: absolute;
      top: 12px;
      right: 12px;
      background-color: rgba(255, 255, 255, 0.9);
      color: var(--amaranth);
      font-size: 0.75rem;
      font-weight: 700;
      padding: 0.3rem 0.8rem;
      border-radius: 20px;
      text-transform: uppercase;
    }

    .product-info {
      padding: 1.5rem;
      display: flex;
      flex-direction: column;
      flex-grow: 1;
    }

    .product-title {
      font-size: 1.2rem;
      margin-bottom: 0.4rem;
    }

    .product-price {
      font-size: 1.1rem;
      color: var(--thulian-pink);
      font-weight: 700;
      margin-bottom: 1rem;
    }

    .product-btn {
      margin-top: auto;
      background-color: var(--chalk);
      color: var(--amaranth);
      border: none;
      padding: 0.6rem;
      border-radius: 12px;
      font-weight: 700;
      cursor: pointer;
      transition: var(--transition);
      width: 100%;
    }

    .product-btn:hover {
      background-color: var(--amaranth);
      color: white;
    }

    /* --------------------------------------------------
       CUSTOMIZATION FORM SECTION
    -------------------------------------------------- */
    .custom-section {
      background-color: var(--card-bg);
      border-radius: 30px;
      padding: 3.5rem 3rem;
      box-shadow: var(--shadow);
      border: 3px solid var(--chalk);
      margin-bottom: 6rem;
    }

    .custom-container {
      display: grid;
      grid-template-columns: 1fr 1fr;
      gap: 3rem;
      align-items: start;
    }

    .form-group {
      margin-bottom: 1.5rem;
    }

    .form-group label {
      display: block;
      font-weight: 700;
      margin-bottom: 0.5rem;
      color: var(--amaranth);
    }

    .form-control {
      width: 100%;
      padding: 0.8rem 1.2rem;
      border: 2px solid var(--chalk);
      border-radius: 12px;
      font-family: inherit;
      font-size: 1rem;
      transition: var(--transition);
      outline: none;
      background-color: #FAFAFA;
    }

    .form-control:focus {
      border-color: var(--thulian-pink);
      background-color: #FFF;
    }

    textarea.form-control {
      resize: vertical;
      min-height: 120px;
    }

    /* Swatches for color selection */
    .color-swatches {
      display: flex;
      gap: 0.8rem;
      margin-top: 0.5rem;
    }

    .swatch {
      width: 32px;
      height: 32px;
      border-radius: 50%;
      cursor: pointer;
      border: 2px solid transparent;
      transition: var(--transition);
    }

    .swatch.selected {
      border-color: var(--text-dark);
      transform: scale(1.15);
    }

    .preview-card {
      background: linear-gradient(180deg, rgba(227, 214, 191, 0.2) 0%, rgba(170, 186, 174, 0.2) 100%);
      border: 2px dashed var(--brook-green);
      border-radius: 20px;
      padding: 2rem;
      text-align: center;
    }

    .preview-box {
      width: 100px;
      height: 100px;
      border-radius: 50%;
      margin: 1.5rem auto;
      display: flex;
      align-items: center;
      justify-content: center;
      font-size: 2.5rem;
      box-shadow: var(--shadow);
      transition: var(--transition);
      background-color: var(--thulian-pink);
    }

    /* Success Message */
    .form-success {
      display: none;
      background-color: var(--brook-green);
      color: white;
      padding: 1rem;
      border-radius: 12px;
      text-align: center;
      margin-top: 1rem;
      font-weight: 600;
    }

    /* --------------------------------------------------
       FOOTER
    -------------------------------------------------- */
    footer {
      background-color: var(--amaranth);
      color: white;
      text-align: center;
      padding: 3rem 1rem;
      margin-top: auto;
    }

    footer p {
      opacity: 0.9;
    }

    footer .heart {
      color: var(--thulian-pink);
    }

    /* Responsive Design */
    @media (max-width: 768px) {
      .custom-container {
        grid-template-columns: 1fr;
      }
      .hero h1 {
        font-size: 2.2rem;
      }
      header {
        flex-direction: column;
        gap: 1rem;
      }
    }
  </style>
</head>
<body>

  <!-- Header Section -->
  <header>
    <div class="logo">
      🧶 Knot & Stitches
    </div>
    <nav>
      <ul>
        <li><a href="#shop">Shop All</a></li>
        <li><a href="#customize">Custom Order</a></li>
        <li><a href="#about">About</a></li>
      </ul>
    </nav>
  </header>

  <!-- Hero Section -->
  <section class="hero">
    <span class="hero-badge">100% Handcrafted Love</span>
    <h1>Cute & Cozy Crochet Creations</h1>
    <p>Bring soft, warm, and colorful handmade goodies into your home. Made with premium yarns and attention to every stitch.</p>
    <a href="#customize" class="btn-primary">Request Custom Design ✨</a>
  </section>

  <main class="container">
    <!-- Filterable Product Section -->
    <section class="products-section" id="shop">
      <h2 class="section-title">Explore Our Gallery</h2>
      <p class="section-subtitle">Find your next cuddly companion or cozy accessory</p>

      <div class="filter-buttons">
        <button class="filter-btn active" data-filter="all">All Items</button>
        <button class="filter-btn" data-filter="plushies">Plushies 🧸</button>
        <button class="filter-btn" data-filter="wearables">Wearables 🧣</button>
        <button class="filter-btn" data-filter="decor">Home Decor 🌸</button>
      </div>

      <div class="product-grid" id="productGrid">
        <!-- Dynamic Products Injected via JavaScript -->
      </div>
    </section>

    <!-- Custom Order Section -->
    <section class="custom-section" id="customize">
      <h2 class="section-title">Custom Order Request</h2>
      <p class="section-subtitle">Have a unique idea? Let's bring it to life together!</p>

      <div class="custom-container">
        <!-- Form -->
        <form id="customForm">
          <div class="form-group">
            <label for="name">Your Name</label>
            <input type="text" id="name" class="form-control" placeholder="e.g. Maya Lin" required>
          </div>

          <div class="form-group">
            <label for="email">Email Address</label>
            <input type="email" id="email" class="form-control" placeholder="hello@example.com" required>
          </div>

          <div class="form-group">
            <label for="itemType">Item Category</label>
            <select id="itemType" class="form-control">
              <option value="Plushie / Amigurumi">Plushie / Amigurumi</option>
              <option value="Beanie / Hat">Beanie / Hat</option>
              <option value="Tote Bag">Tote Bag</option>
              <option value="Coaster / Decor">Coaster / Decor Set</option>
              <option value="Other">Something Else!</option>
            </select>
          </div>

          <div class="form-group">
            <label>Primary Yarn Color Inspiration</label>
            <div class="color-swatches">
              <div class="swatch selected" style="background-color: var(--amaranth);" data-color="#933B5B" data-name="Amaranth"></div>
              <div class="swatch" style="background-color: var(--thulian-pink);" data-color="#B5728A" data-name="Thulian Pink"></div>
              <div class="swatch" style="background-color: var(--brook-green);" data-color="#AABAAE" data-name="Brook Green"></div>
              <div class="swatch" style="background-color: var(--chalk);" data-color="#E3D6BF" data-name="Chalk"></div>
              <div class="swatch" style="background-color: var(--pomelo-olive);" data-color="#9F9679" data-name="Pomelo Olive"></div>
            </div>
            <input type="hidden" id="selectedColor" value="Amaranth">
          </div>

          <div class="form-group">
            <label for="details">Customization Details</label>
            <textarea id="details" class="form-control" placeholder="Describe size, specific characters, colors, or special features you'd like..." required></textarea>
          </div>

          <button type="submit" class="btn-primary" style="width: 100%;">Send Request to Artist 💌</button>
          <div class="form-success" id="formSuccess">Yay! Your request has been sent! We'll reply soon. 🧶</div>
        </form>

        <!-- Dynamic Live Preview Box -->
        <div class="preview-card">
          <h3>Live Request Summary</h3>
          <p style="font-size: 0.9rem; color: #666; margin-top: 0.2rem;">Here's a preview of your request:</p>
          
          <div class="preview-box" id="previewIcon">
            🧸
          </div>

          <p><strong>Category:</strong> <span id="previewCategory">Plushie / Amigurumi</span></p>
          <p><strong>Color Tone:</strong> <span id="previewColorName">Amaranth</span></p>
        </div>
      </div>
    </section>
  </main>

  <footer>
    <p>Made with 💕 & Yarn | Knot & Stitches Crochet Studio &copy; 2026</p>
  </footer>

  <script>
    /* --------------------------------------------------
       PRODUCT DATA & FILTERING
    -------------------------------------------------- */
    const products = [
      { id: 1, name: "Berry Amigurumi Bear", category: "plushies", price: "$28.00", icon: "🧸" },
      { id: 2, name: "Pastel Meadow Beanie", category: "wearables", price: "$22.00", icon: "🧢" },
      { id: 3, name: "Cozy Flower Coaster Set", category: "decor", price: "$16.00", icon: "🌸" },
      { id: 4, name: "Strawberry Crossbody Bag", category: "wearables", price: "$34.00", icon: "👛" },
      { id: 5, name: "Sleepy Mushroom Pal", category: "plushies", price: "$25.00", icon: "🍄" },
      { id: 6, name: "Sage Market Tote", category: "wearables", price: "$30.00", icon: "🛍️" },
      { id: 7, name: "Chunky Daisy Pillow", category: "decor", price: "$38.00", icon: "🌼" },
      { id: 8, name: "Mini Frog Keychain", category: "plushies", price: "$12.00", icon: "🐸" }
    ];

    const productGrid = document.getElementById('productGrid');
    const filterButtons = document.querySelectorAll('.filter-btn');

    function renderProducts(filter = 'all') {
      productGrid.innerHTML = '';
      
      const filteredProducts = filter === 'all' 
        ? products 
        : products.filter(p => p.category === filter);

      filteredProducts.forEach(product => {
        const card = document.createElement('div');
        card.className = 'product-card';
        card.innerHTML = `
          <div class="product-img">
            <span>${product.icon}</span>
            <span class="product-tag">${product.category}</span>
          </div>
          <div class="product-info">
            <h3 class="product-title">${product.name}</h3>
            <p class="product-price">${product.price}</p>
            <button class="product-btn" onclick="prefillCustom('${product.name}')">Adopt / Customize</button>
          </div>
        `;
        productGrid.appendChild(card);
      });
    }

    filterButtons.forEach(btn => {
      btn.addEventListener('click', () => {
        filterButtons.forEach(b => b.classList.remove('active'));
        btn.classList.add('active');
        renderProducts(btn.dataset.filter);
      });
    });

    // Initial Render
    renderProducts();

    /* --------------------------------------------------
       CUSTOMIZATION FORM & PREVIEW INTERACTION
    -------------------------------------------------- */
    const itemTypeSelect = document.getElementById('itemType');
    const previewCategory = document.getElementById('previewCategory');
    const previewIcon = document.getElementById('previewIcon');
    const swatches = document.querySelectorAll('.swatch');
    const selectedColorInput = document.getElementById('selectedColor');
    const previewColorName = document.getElementById('previewColorName');
    const customForm = document.getElementById('customForm');
    const formSuccess = document.getElementById('formSuccess');

    // Update Category Preview
    itemTypeSelect.addEventListener('change', (e) => {
      previewCategory.textContent = e.target.value;
      
      // Dynamic Icon Update based on selection
      if(e.target.value.includes('Plushie')) previewIcon.textContent = '🧸';
      else if(e.target.value.includes('Hat')) previewIcon.textContent = '🧢';
      else if(e.target.value.includes('Tote')) previewIcon.textContent = '🛍️';
      else if(e.target.value.includes('Decor')) previewIcon.textContent = '🌸';
      else previewIcon.textContent = '✨';
    });

    // Swatch Color Picker
    swatches.forEach(swatch => {
      swatch.addEventListener('click', () => {
        swatches.forEach(s => s.classList.remove('selected'));
        swatch.classList.add('selected');
        
        const colorHex = swatch.dataset.color;
        const colorName = swatch.dataset.name;
        
        selectedColorInput.value = colorName;
        previewColorName.textContent = colorName;
        previewIcon.style.backgroundColor = colorHex;
      });
    });

    // Quick-link button to prefill custom details
    function prefillCustom(productName) {
      document.getElementById('details').value = `I'd like to request a custom version of the ${productName}! Here are my ideas: `;
      document.getElementById('customize').scrollIntoView({ behavior: 'smooth' });
    }

    // Form Submission Handler
    customForm.addEventListener('submit', (e) => {
      e.preventDefault();
      
      // Visual feedback
      formSuccess.style.display = 'block';
      customForm.reset();
      
      // Reset defaults
      previewCategory.textContent = 'Plushie / Amigurumi';
      previewColorName.textContent = 'Amaranth';
      previewIcon.style.backgroundColor = 'var(--thulian-pink)';
      previewIcon.textContent = '🧸';

      setTimeout(() => {
        formSuccess.style.display = 'none';
      }, 5000);
    });
  </script>
</body>
</html>
